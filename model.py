import Dictionary from dict_class


main()
    display commands
    load dictionaries
    if dictionary isWorking
        mount dict (create obj)
        display dict
    display len(dict)
    while True command loop
        add = self.mounted_dictionary.add()
        def
        del
        flash
        move from default="mount" -> (".name")
        test_mode(optional index) <local>
        monut(dict) <local>
        new   Dictionary(name)

        ===========
        commands
        clear
        quit > q
        
        
if __name__ == "__main__":
    main()
        
    
    
    
